import type { Component } from "solid-js";

const Caching: Component = () => {
    return(
        <div class="article">
            <p>
                Caching is a way of holding on to the data faster than storing it on the database 
            </p>
        </div>
    )
}

export default Caching;